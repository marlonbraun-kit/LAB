# MoveIt controller manager bindings.
#
# Tells MoveIt which ros2_control controllers to drive when executing a
# planned trajectory. The names below must match the controller names in
# config/ur3_controllers.yaml.

moveit_controller_manager: moveit_simple_controller_manager/MoveItSimpleControllerManager

moveit_simple_controller_manager:
  controller_names:
    - scaled_joint_trajectory_controller

  scaled_joint_trajectory_controller:
    type: FollowJointTrajectory
    action_ns: follow_joint_trajectory
    default: true
    joints:
      - shoulder_pan_joint
      - shoulder_lift_joint
      - elbow_joint
      - wrist_1_joint
      - wrist_2_joint
      - wrist_3_joint
  # Zimmer HRC-03 gripper is driven via /io_and_status_controller/set_io
  # directly from pick_place_manager_node, not through MoveIt.
